[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "adaptive-plato"
version = "0.1.0"
description = "Adaptive Plato early version"
requires-python = ">=3.8"

[tool.setuptools]
py-modules = ["adaptive_plato"]
